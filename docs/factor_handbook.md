# Growth OS 因子手册

> 版本：v2.1.1 | 更新：2026-05-26

---

## 架构总览

```
L0 风格择时门控（regime.py）
    ├─ GROWTH_OK / CAUTION → Growth OS 五层漏斗（L1-L5）
    └─ DEFENSE             → 防御资产篮子（defense.py）
                                40% 中证红利 + 40% 国债ETF + 20% 现金
```

漏斗流程（GROWTH_OK / CAUTION 时）：

```
全市场5000+ → 市值Top800候选池 → L1排雷 → L2护城河 → L3资本效率 → L4行业校准 → L5预期差 → 综合分排序 → Top30
```

每层 0-10 分，按生命周期阶段加权合成综合分（满分 100）。候选池内做**截面排名标准化**消除层间方差。

---

## L0：风格择时门控（v2.0 新增）

`growth_os/regime.py` — 三通道信号 → 三态输出 → 控制是否启用成长漏斗。

### 三通道信号

| 通道 | 信号 | 计算 | 阈值 |
|------|------|------|------|
| A 成长相对强度 | 创业板/沪深300 63日动量斜率 | `np.polyfit(rel_curve, 1)` | 斜率 < 0 = 触发 |
| B 利率边际变化 | 10Y国债 63日变动 | `yield[-1] - yield[-64]` | > +30bp 且 > 200日均值 |
| C 风格回撤熔断 | 创业板 126日最大回撤 | `1 - price / cummax` | > 20% 且 价格 < MA63 |

### 三态输出

| 状态 | 触发条件 | 动作 | K |
|------|----------|------|---|
| **GROWTH_OK** | 无通道触发 | 正常漏斗，生命周期权重 | 30 |
| **CAUTION** | 单通道触发 | 正常漏斗，防御权重 + l1_strict + g×0.7 | 30 |
| **DEFENSE** | A+C 或 B+C 或 A+B | **跳过漏斗，切防御资产** | — |

### 去抖规则

- 进入 DEFENSE：多通道触发即时生效（不等，应对急跌）
- 退出 DEFENSE：1 期 GROWTH_OK 即可退出
- CAUTION ↔ GROWTH_OK：2 期确认

### 防御资产配置

`growth_os/defense.py`：

| 资产 | 代理 | 权重 | 数据源 |
|------|------|------|--------|
| 中证红利 | 000922 指数 | 40% | akshare `stock_zh_index_daily` |
| 国债 ETF | 511010 | 40% | akshare `fund_etf_hist_em` |
| 现金 | 年化 1.5% | 20% | 固定收益 |

---

## 0. 生命周期判定（权重分配的前置条件）

四阶段分类，由 `classify_lifecycle()` (`growth_os/lifecycle.py`) 判定，带 **2 期锁定期防抖**。

| 阶段 | 判定条件 | 权重侧重 |
|------|----------|----------|
| **导入期** | 营收>0 且 (扣非利润<0 或 净利率<1%) | L2(30%) + L4(35%) |
| **加速期** | 营收3年CAGR≥15% + 扣非增速≥营收×0.8 + 毛利率不降 | L2(30%) + L3(25%) |
| **成熟期** | ROIC>WACC + FCF>0 + 增速放缓 | L3(40%) |
| **衰退期** | 毛利率连续2季下滑+应收/存货异常；或 ROIC<WACC+费用恶化+营收负增 | 一票否决 |

### 动态权重矩阵

| 阶段 | L1排雷 | L2护城河 | L3效率 | L4行业 | L5预期差 |
|------|--------|----------|--------|--------|----------|
| 导入期 | 0.10 | 0.30 | 0.10 | 0.35 | 0.15 |
| 加速期 | 0.20 | 0.30 | 0.25 | 0.15 | 0.10 |
| 成熟期 | 0.15 | 0.20 | 0.40 | 0.15 | 0.10 |

### Regime 防御权重（CAUTION 时使用）

| 权重模式 | L1排雷 | L2护城河 | L3效率 | L4行业 | L5预期差 |
|----------|--------|----------|--------|--------|----------|
| defensive | 0.25 | 0.15 | 0.40 | 0.10 | 0.10 |
| maturity_forced | 0.25 | 0.15 | 0.40 | 0.10 | 0.10 |

---

## L1：排雷（Hard Gate）

二级红灯制。**绝对红灯 1 项即淘汰**，条件红灯 ≥2 项淘汰（l1_strict 时 ≥1 项淘汰），1 项进观察池。

> v2.1：pdfplumber/camelot 已安装，全部 11 项红灯生效。

### 绝对红灯（单灯淘汰）

| # | 因子 | 字段 | 阈值 | 逻辑 |
|---|------|------|------|------|
| 1 | 无财务数据 | `_error` | — | 数据缺失，无法评估 |
| 2 | 商誉/净资产 | `goodwill_ratio` | >30% | 并购地雷 |
| 3 | 应收飙升 | `receivable_surge` | 应收增速 > 营收增速×1.5 | 恶性赊销 |
| 4 | 存货结构 | `inventory_structure` | 产成品占比 >70% | 滞销积压（PDF附录） |
| 5 | 研发资本化率 | `rd_capitalization` | >50% | 虚增利润（PDF附录） |
| 6 | 政府补助依赖 | `subsidy_dependency` | 补助/扣非利润 >50% | 非经常性依赖（PDF附录） |

### 条件红灯

| # | 因子 | 字段 | 阈值 | 逻辑 |
|---|------|------|------|------|
| 7 | 营收3年CAGR | `revenue_cagr_3y` | <10% | 绝对营收TTM计算 |
| 8 | 扣非增速 vs 营收 | `deducted_vs_revenue` | 扣非增速 < 营收增速×0.5 | 利润跟不上收入 |
| 9 | OCF/净利润(3Y) | `ocf_profit_ratio_3y` | <0.6 | 现金含金量 |
| 10 | 存货飙升 | `inventory_surge` | 存货增速 > 营收增速×1.5（重资产×2.0） | 库存积压 |
| 11 | 现金流燃烧 | `cashflow_burn` | 近3季OCF<0 且 利润>0 | 应收驱动的虚增利润 |

**CAUTION/DEFENSE 时 `l1_strict=True`：条件红灯 ≥1 项即淘汰。**

---

## L2：护城河（0-10 分）

| # | 因子 | 满分 | 计算方式 |
|---|------|------|----------|
| 1 | 毛利率趋势 | 3.0 | 近4季均值vs前4季：上升(>+2%)满分，稳定(±2%)半满分 |
| 2 | 费用率杠杆 | 2.0 | (销售+管理)费用率：下降>5%满分，持平半满分 |
| 3 | 研发强度 | 2.0 | 研发/营收 vs 行业`rd_intensity_high` |
| 4 | 合同负债领先 | 2.0 | 合同负债增速 vs 营收增速；白酒/军工行业×1.5 |
| 5 | 营收二阶导数 | 1.0 | 营收YoY连续环比提升：≥2季满分 |
| 6 | 高毛利业务占比 | +1.0 | PDF附录增强，>50%加成1分 |

---

## L3：资本效率（0-10 分）

| # | 因子 | 满分 | 计算方式 |
|---|------|------|----------|
| 1 | ROIC vs WACC | 4.0 | 利差>5pp满分，>0得2.5，<0得0 |
| 2 | ROIC趋势 | 2.0 | 同期对比（最新FY vs 上一FY） |
| 3 | ROE质量 | 2.0 | ROE>20%满分；**权益乘数>5分数减半** |
| 4 | FCF趋势 | 1.0 | 每股FCF>0满分 |
| 5 | 债务安全 | 1.0 | 有息负债率<30% 且 利息覆盖>5x |

### WACC 计算

| 参数 | 值 | 说明 |
|------|-----|------|
| Beta回归窗口 | 504交易日（≈2年） | vs 沪深300 |
| ERP方法 | blend | Damodaran A股ERP + 收益法综合 |
| 无风险利率 | 10年国债 | akshare实时获取 |

---

## L4：行业校准（0-10 分）

核心逻辑：同申万三级行业内**百分位排名**，非绝对阈值。

### 行业内排名（基础分）

| # | 因子 | 权重 | 方向 |
|---|------|------|------|
| 1 | 毛利率 | 25% | 正向 |
| 2 | 营收增速 | 20% | 正向 |
| 3 | 净利率 | 20% | 正向 |
| 4 | ROIC | 20% | 正向 |
| 5 | 有息负债率 | 15% | 反向（越低越好） |

加权百分位 ÷ 10 = 基础分。

### 行业特征加成

| 类型 | 加成 | 涵盖行业 |
|------|------|----------|
| 主动备产型 | +0.5 | 半导体设备/制造、光伏、锂电 |
| 订单先行型 | +0.5 | 白酒、军工（航空/电子/航天）、SaaS |
| 研发驱动型 | +0.5 | 创新药、半导体材料、软件 |
| 利润容忍型 | +1.0 | 化学制剂、生物制品、医疗设备 |
| 毛利率敏感型 | +0.5 | 消费电子 |

**完整行业配置**：`INDUSTRY_ADJUSTMENTS` in `growth_os/config.py`，当前覆盖 22 个申万三级行业。

---

## L5：预期差（0-10 分）

### 前瞻增速 g_proxy（PEG 的 G）

指数加权近4季营收YoY（λ=0.55），经交叉验证：

- **合同负债验证**：合同负债增速>0 → `g = max(g_ewa, cl_growth×0.8)`
- **OCF含金量折扣**：OCF/净利润<0.75 → `g×0.7`，<0.95 → `g×0.9`
- **前瞻修正（v2.1）**：近2季YoY均值低于前2季 → `g×max(recent/older, 0.6)`，防止历史高增速在拐点处被线性外推
- 输出范围钳位：[4%, 80%]

### 因子

| # | 因子 | 满分 | 计算方式 |
|---|------|------|----------|
| 1 | PEG | 4.0 | PE / g_proxy；<1.5满分，1.5-2.5得2.5，2.5-3.0得1.0 |
| 2 | PE行业内分位 | 3.0 | 同行业PE对比；分位<30%满分 |
| 3 | 增长加速度 | 3.0 | 近2季YoY均值 vs 前2季；>1.1x满分 |

---

## 综合得分

```
综合分 = L1_risk×w1×10 + s2×w2×10 + s3×w3×10 + s4×w4×10 + s5×w5×10
```

- `L1_risk = max(0, 10 - 红灯数)`
- `s2/s3/s5` = 标准化rank×10（候选池内百分位→[0.05,0.95]→0-10）
- `s4` = L4原始分（本身已是行业百分位）
- `w1-w5` = 生命周期动态权重
- CAUTION/DEFENSE 时 `w1-w5` = REGIME_WEIGHTS 覆盖

**决策阈值**：
- ≥70：深度研究
- 50-70：加入观察池
- <50：暂不关注
- L1否决：一票否决

---

## 因子总表

| 层级 | 类型 | 因子数 | 满分 | 核心 |
|------|------|--------|------|------|
| L0 风格择时 | gate | 3通道 | — | 成长相对强度+利率+回撤熔断 |
| L0 防御资产 | 替代 | 3资产 | — | 40%红利+40%国债+20%现金 |
| 生命周期 | 元因子 | 1 | — | 四阶段+LOCK_PERIODS=2 |
| L1 排雷 | gate | 11 | — | 6绝对+5条件，二级红灯制 |
| L2 护城河 | 评分 | 6 | 10 | 毛利趋势+费用杠杆+研发+合同负债+营收二阶导+业务升级 |
| L3 资本效率 | 评分 | 5 | 10 | ROIC-WACC+ROIC趋势+ROE(去杠杆)+FCF+债务安全 |
| L4 行业校准 | 评分 | 5+行业加成 | 10 | 同业百分位排名(5指标加权)+行业特征加成 |
| L5 预期差 | 评分 | 3 | 10 | PEG(g_proxy)+PE分位+增长加速度 |
| 截面标准化 | 后处理 | 1 | — | L2/L3/L5 百分位→[0.05,0.95]拉伸 |
| **合计** | | **39** | | 含 L0 6项 |

---

## 回测结果

### 样本内（2022 Q1 → 2025 Q1）

| 指标 | v1.3（基线） | v2.1（+L0+防御+PDF+g_proxy） | 改善 |
|------|-------------|----------------------------|------|
| 12M 超额 | +6.2% | **+7.8%** | +1.6pp |
| 12M Calmar | 0.92 | **5.35** | ×5.8 |
| 最大回撤 | 71.2% | **30.2%** | -41pp |
| 2022 年 | -20.4% | **-6.6%** | +13.8pp |
| 2023 年 | -6.6% | **+2.2%** | +8.8pp |
| 2024 年 | +35.4% | +25.2% | -10.2pp |

### 样本外验证（2019 Q1 → 2020 Q4）

| 指标 | 结果 | 判定 |
|------|------|------|
| 12M 超额 | **+5.5%** | ✅ 显著为正 |
| 2019 年 | +21.4% | ✅ 成长牛市正常进攻 |
| 2020 年 | +18.4% | ✅ 疫情V型反弹捕获 |
| DEFENSE 误切 | 仅 2019Q2（1/8） | ✅ 未误杀 2020 牛市 |
| Regime 分布 | 75% GROWTH_OK | ✅ 无过拟合 |

**结论：v2.1 通过样本外验证，regime 信号具备泛化能力，不是 2022 年以后专用策略。**

---

## 关键代码位置

| 模块 | 文件 | 核心函数 |
|------|------|----------|
| **风格择时门控** | `growth_os/regime.py` | `compute_regime()`, `_channel_a/b/c()` |
| **防御资产** | `growth_os/defense.py` | `get_defense_basket_return()` |
| 生命周期判定 | `growth_os/lifecycle.py` | `classify_lifecycle()`, `resolve_weights()` |
| 五层漏斗 | `growth_os/funnel.py` | `run_funnel()`, `_check_l1()`, `_score_l2~5()` |
| 打分卡 | `growth_os/scorecard.py` | `compute_composite()`, `normalize_pool()` |
| 参数配置 | `growth_os/config.py` | 所有阈值、权重、行业调整、REGIME_WEIGHTS |
| WACC计算 | `growth_os/wacc.py` | `compute_wacc()` |
| 数据加载 | `growth_os/data.py` | `load_growth_data()`, `get_financial_snapshot()` |
| 回测引擎 | `growth_os/backtest.py` | `run_backtest()` |
| 批量筛选 | `growth_os/run_screen.py` | `screen_all()` |

---

## 迭代历史

| 版本 | 尝试 | 本质 | 结论 |
|------|------|------|------|
| v1.0-v1.3 | 33因子五层漏斗 | 成长纯度引擎 | 71%回撤，无法实盘 |
| v2.0-dev1 | DEFENSE K缩至8 | "少买=防守" | 更差——集中押注最脆弱的成长股 |
| v2.0-dev2 | DEFENSE切换权重 | "换排序=换物种" | 几乎无效——33因子全是成长导向 |
| **v2.0** | **DEFENSE切资产类别** | **物理切断Beta暴露** | **Calmar 5.35，回撤30%** |
| v2.1-dev | DEFENSE+周期鉴别 | 过滤伪加速期 | 误杀真成长，2024损失9pp |
| **v2.1** | **PDF因子恢复+g_proxy修正** | **低风险增强** | 无退化，L1恢复11项，拐点保护 |

**核心认知：Growth OS 是成长纯度引擎，不是全天候策略。风控来自物理切断 Beta，而非在成长池内精挑细选。**

### 样本外验证（2019-2021）

v2.1 在完全未见的数据上通过验证：12M超额+5.5%，DEFENSE仅在2019Q2触发一次，2020年牛市全程GROWTH_OK。regime信号具备泛化能力，不存在对2022年以后数据的过拟合。

---

## 已知问题 / 待迭代

1. **周期鉴别未上线**：当前阈值（毛利率波动率×2.0、PPI相关>0.6）太粗，误杀真成长。需更精细的行业校准后重新评估，当前降级为 Research Track。
2. **WACC小市值不稳定**：小市值公司Beta回归可能不稳定，可考虑行业Beta中位数替代。
3. **2024年退出延迟**：Q1-Q2仍在DEFENSE，牺牲约10pp超额。未来可增加"恢复期渐进式回归"机制（RECOVERY中间态）。
4. **g_proxy仍无分析师预期修正**：当前仅用历史YoY外推+减速惩罚，未来可接入一致预期数据增强前瞻精度。

### 已修复

- ✅ PDF附录因子（v2.1）：L1红灯从7项恢复至11项
- ✅ g_proxy拐点滞后（v2.1）：近2季YoY连续回落→渐进惩罚
- ✅ 风格Beta暴露（v2.0）：DEFENSE资产切换物理切断
- ✅ 最大回撤失控（v2.0）：71.2%→30.2%

---

## v2.1.1 新增：L5 数据完整性机制

L5 评分三态化（`growth_os/scorecard.py` — `L5Status` 枚举）：

| 状态 | 条件 | 处理 |
|------|------|------|
| **OK** | PE/PEG 完整可用 | 正常参与综合分，满分 10 |
| **PARTIAL** | 仅增长加速度可用（PE/PEG 缺失） | L5 上限锁定 5/10 |
| **MISSING** | 全部不可用 | L5 不参与综合分，L1-L4 权重重归一化 |

报告中显式标注估值状态（`growth_os/report.py`）：
- ✅ 估值可评估 — PEG=X.X, PE分位=XX%
- ⚠️ 估值数据不完整 — 仅增长加速度可用，上限 5/10
- 🔴 估值不可用 — L5 未参与综合分，请人工核实

PE 数据修复（`growth_os/data.py`）：
- `get_price_data()` 查询优先级调整：桌面文件（含 `peTTM` 列）> 缓存目录 > akshare

---

## 当前问题与优化路线图

### 已修复

| # | 问题 | 版本 |
|---|------|------|
| 1 | PE/PEG 数据缺失 → L5 交白卷 | v2.1.1 |
| 2 | 数据缺失伪装成 0 分 → 用户被误导 | v2.1.1 |
| 3 | PDF 因子不可用，L1 仅 7 项 | v2.1 |
| 4 | g_proxy 拐点滞后 | v2.1 |
| 5 | 71% 最大回撤 | v2.0 |
| 6 | Regime 择时净贡献 -5.5pp | 待优化 |

### 近期待优化（v2.1.x）

| 优先级 | 事项 | 工时 | 说明 |
|--------|------|------|------|
| **P0** | L5 边界声明 | 1h | PEG 框架对强周期行业可能失效，报告中应标注适用域 |
| **P0** | 行业阈值校准 | 2h | 通信设备/硬件成长股 `rd_intensity` 阈值需下调至 5-6% |
| **P1** | 双分数输出 | 1d | 拆为"成长质量分(L1-L4)"+"估值安全边际(L5)"，避免质量与价格混为一谈 |
| **P1** | PE 历史分位 | 1d | 当前只做行业排位，缺少自身历史维度（80 倍 PE 在历史上处于什么位置） |

### 远期架构（需设计讨论）

| 优先级 | 事项 | 说明 |
|--------|------|------|
| **P2** | 增长持续性判断 | 当前系统无法区分"技术渗透型增长（10年）"和"价格周期型增长（2年）"。纯因子方法已证明误杀太多，需行业叙事模板或二阶导护栏 |
| **P2** | 多维决策矩阵 | 输出 Q(质量)×V(估值)×N(叙事) 的三维决策空间，替代单一综合分 |
| **P2** | Regime 连续化 | v2.2-dev 证明 RECOVERY 架构正确但离散参数极敏感。需从状态机升级为 0-100% 连续仓位函数 |
| **P2** | 报告置信度体系 | 每个模块应输出数据完整度和结论置信度，让用户知道哪里可信、哪里不确定 |

### 核心认知（v2.1.1 验证）

> 旭创 300308 案例证明：系统最大的问题不是"算不准"，而是"把不知道伪装成知道"。v2.1.1 的 L5Status 机制解决了这个诚信问题——估值数据缺失时显式标注"无法评估"，而不是偷偷给 0 分。这是系统从"工具"走向"决策伙伴"的关键一步。

### 已放弃的方向

| 方向 | 版本 | 结论 |
|------|------|------|
| K 缩防御（DEFENSE 30→8） | v2.0-dev1 | 更差——集中押注最脆弱的成长股 |
| 权重切换（成熟期权重） | v2.0-dev2 | 几乎无效——33 因子全是成长导向 |
| 纯因子周期鉴别 | v2.1-dev | 误杀真成长，2024 损失 9pp |
| 离散 RECOVERY 状态 | v2.2-dev | 架构正确但参数极度敏感，需升级为连续仓位 |

# Cyclical-Driver-System 依赖清单
# 安装: uv pip install -r requirements.txt --python .venv/bin/python
# 或:   python -m pip install -r requirements.txt

# ---- 核心 ----
pandas>=2.0.0
numpy>=1.24.0

# ---- 数据源 ----
akshare>=1.12.0        # 行情/财务/宏观 API
pytdx>=1.0             # 通达信财务数据解析 (gpcw*.dat)

# ---- 日志 ----
loguru>=0.7.0

# ---- 测试 (Gate 1) ----
pytest>=8.0             # 测试框架

# ---- 统计分析 (signals/diagnostics) ----
scipy>=1.10.0           # spearmanr 等
statsmodels>=0.14.0     # 回归分析

# ---- 可视化 (growth_os/visualization.py, diagnostics) ----
matplotlib>=3.7.0

# ---- 间接依赖 (akshare 可选但项目实际使用) ----
openpyxl>=3.1.0         # akshare Excel 数据
beautifulsoup4>=4.12.0  # akshare HTML 解析
lxml>=4.9.0             # akshare HTML 解析
requests>=2.31.0
